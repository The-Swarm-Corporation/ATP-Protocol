# Copy to ".env" and fill in values.
# Do NOT commit your real ".env" (it is gitignored).

# --- Required ---

# API key used to call the upstream Swarms agent execution endpoint.
SWARMS_API_KEY=your_swarms_api_key_here

# Solana public key (base58) that receives user payments for agent execution.
AGENT_TREASURY_PUBKEY=YOUR_SOLANA_TREASURY_PUBLIC_KEY


# --- Optional (defaults are shown) ---

# Solana RPC URL used for transaction verification.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Redis connection URL (used for temporary job/result storage).
REDIS_URL=redis://localhost:6379/0

# How long (in seconds) a payment challenge stays valid before expiring.
JOB_TTL_SECONDS=600

# Logging verbosity (used by container/runtime). Typical values: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
